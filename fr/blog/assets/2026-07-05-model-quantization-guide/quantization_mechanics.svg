<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 850 520" width="100%" height="100%" role="img" aria-label="Quantization Mechanics: Weight-Only vs. Weight-Activation" data-visual-system="evidence-plate-v1" preserveAspectRatio="xMidYMid meet" shape-rendering="geometricPrecision" text-rendering="geometricPrecision" font-family="Inter, system-ui, sans-serif">
  <title id="artifact-title">Mécanique de quantification : uniquement les poids versus poids et activation</title>
  <defs>
    <!-- Drop Shadow Filter -->
    <filter id="drop-shadow" x="-5%" y="-5%" width="115%" height="115%">
      <feDropShadow dx="0" dy="2" stdDeviation="2" flood-color="#334155" flood-opacity="0.08" />
    </filter>
    <!-- Gradients -->
    <linearGradient id="blue-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#2563eb" />
      <stop offset="100%" stop-color="#2563eb" />
    </linearGradient>
    <linearGradient id="purple-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#7c3aed" />
      <stop offset="100%" stop-color="#7c3aed" />
    </linearGradient>
    <linearGradient id="orange-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ea580c" />
      <stop offset="100%" stop-color="#ea580c" />
    </linearGradient>
    <linearGradient id="green-grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#16a34a" />
      <stop offset="100%" stop-color="#16a34a" />
    </linearGradient>
  </defs>
  <style data-visual-system="evidence-plate-v1">
    * { animation: none !important; }
    .anim-box, .bar, .bar-text, .seq-item, .t-box, .flow { opacity: 1 !important; transform: none !important; }
    path, line, polyline { stroke-linecap: round; stroke-linejoin: round; }
  </style>

  <!-- Background -->
  <rect width="850" height="520" fill="#f8fafc" rx="12" />

  <!-- Title -->
  <text y="40" font-family="Inter, system-ui, sans-serif" font-size="20" font-weight="700" fill="#0f172a" x="30">
    Seuils uniquement (W4A16) versus seuils avec activation (W8A8/FP8)
  </text>
  <text y="60" font-family="Inter, system-ui, sans-serif" font-size="12" fill="#334155" x="30">
    Comparaison du transfert de mémoire GPU, du traitement des registres et de l’utilisation des cœurs tensoriels
  </text>

  <!-- Divider Line -->
  <line x1="425" y1="90" x2="425" y2="490" stroke="#cbd5e1" stroke-dasharray="5,5" stroke-width="2" />

  <!-- SECTION 1: WEIGHT-ONLY (W4A16) -->
  <g>
    <!-- Subtitle -->
    <rect x="30" y="90" width="365" height="30" fill="#eff6ff" rx="6" />
    <text y="110" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="12.5" font-weight="700" fill="#1d4ed8" x="212.5">
      A. Quantification uniquement basée sur les poids (par ex. W4A16 GPTQ/AWQ)
    </text>
    <text y="142" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="11" fill="#334155" x="212.5">
      Optimise la décodage limité en mémoire (petits lots / flux unique)
    </text>

    <!-- GPU VRAM (HBM) -->
    <rect x="50" y="160" width="320" height="75" fill="#ffffff" stroke="#cbd5e1" stroke-width="1.5" rx="8" />
    <text y="180" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="11" font-weight="700" fill="#334155" x="210">GPU VRAM (HBM)</text>

    <!-- weights box -->
    <rect x="65" y="192" width="135" height="30" fill="url(#orange-grad)" rx="4" />
    <text y="211" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="11" font-weight="600" fill="#ffffff" x="132.5">Poids (4 bits)</text>
    <!-- activations box -->
    <rect x="220" y="192" width="135" height="30" fill="url(#blue-grad)" rx="4" />
    <text y="211" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="11" font-weight="600" fill="#ffffff" x="287.5">Activations (16 bits BF16)</text>

    <!-- Data Flow Arrows -->
    <path d="M 132.5 235 L 132.5 272" fill="none" stroke="#ea580c" stroke-width="2.5" marker-end="url(#arrow-orange)" />
    <path d="M 287.5 235 L 287.5 272" fill="none" stroke="#2563eb" stroke-width="2.5" marker-end="url(#arrow-blue)" />

    <!-- GPU Compute Block -->
    <rect x="50" y="285" width="320" height="150" fill="#ffffff" stroke="#94a3b8" stroke-width="1.5" rx="8" />
    <text y="305" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="11" font-weight="700" fill="#334155" x="210">Exécution en SRAM/registre</text>

    <!-- Dequantization Step -->
    <rect x="65" y="320" width="135" height="40" fill="#fff7ed" stroke="#ea580c" stroke-width="1.5" rx="4" />
    <text y="337" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="10" font-weight="700" fill="#9a3412" x="132.5">Déquantification en kernel</text>
    <text y="350" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="9" fill="#9a3412" x="132.5">INT 4 bits → FP 16 bits</text>

    <!-- Activations path (Unchanged) -->
    <rect x="220" y="320" width="135" height="40" fill="#eff6ff" stroke="#2563eb" stroke-width="1.5" rx="4" />
    <text y="337" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="10" font-weight="700" fill="#1d4ed8" x="287.5">Conserver en 16 bits</text>
    <text y="350" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="9" fill="#1d4ed8" x="287.5">Non-déquantification</text>

    <!-- Arrow from Dequant to Tensor Core -->
    <path d="M 132.5 360 L 132.5 388 C 132.5 400, 180 400, 195 400" fill="none" stroke="#ea580c" stroke-dasharray="3,3" stroke-width="1.5" />

    <!-- Activations entry line -->
    <path d="M 287.5 285 L 287.5 320" fill="none" stroke="#2563eb" stroke-width="1.5" />

    <!-- Activations bypass dequant -->
    <path d="M 287.5 360 L 287.5 388 C 287.5 400, 240 400, 225 400" fill="none" stroke="#2563eb" stroke-width="1.5" />

    <!-- Math execution box -->
    <rect x="180" y="385" width="60" height="35" fill="url(#purple-grad)" rx="4" />
    <text y="406" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="11" font-weight="700" fill="#ffffff" x="210">BF16 TCT</text>
    <text y="445" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="10" fill="#334155" x="210">La mathématique des poids est effectuée en 16 bits</text>

    <!-- Bottom Note -->
    <rect x="50" y="450" width="320" height="40" fill="#fff7ed" stroke="#ea580c" rx="6" />
    <text y="466" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="10" font-weight="600" fill="#9a3412" x="210">⚠️ Le goulot d’étranglement de calcul reste actif :</text>
    <text y="478" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="9.5" fill="#9a3412" x="210">Des VRAM sont obtenus, mais il n’y a pas d’accélération du calcul.</text>
  </g>

  <!-- SECTION 2: WEIGHT-ACTIVATION (W8A8/FP8) -->
  <g transform="translate(425, 0)">
    <!-- Subtitle -->
    <rect x="30" y="90" width="365" height="30" fill="#f0fdf4" rx="6" />
    <text y="110" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="12.5" font-weight="700" fill="#166534" x="212.5">
      B. Quantification de l’activation en fonction du poids (W8A8 / FP8)
    </text>
    <text y="142" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="11" fill="#334155" x="212.5">
      Accélère les tâches gourmandes en calcul Prefill (haute concurrence / Serving)
    </text>

    <!-- GPU VRAM (HBM) -->
    <rect x="50" y="160" width="320" height="75" fill="#ffffff" stroke="#cbd5e1" stroke-width="1.5" rx="8" />
    <text y="180" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="11" font-weight="700" fill="#334155" x="210">GPU VRAM (HBM)</text>

    <!-- weights box -->
    <rect x="65" y="192" width="135" height="30" fill="url(#green-grad)" rx="4" />
    <text y="211" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="11" font-weight="600" fill="#ffffff" x="132.5">Poids (8 bits)</text>
    <!-- activations box -->
    <rect x="220" y="192" width="135" height="30" fill="url(#green-grad)" rx="4" />
    <text y="211" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="11" font-weight="600" fill="#ffffff" x="287.5">Activations (8 bits)</text>

    <!-- Data Flow Arrows -->
    <path d="M 132.5 235 L 132.5 280" fill="none" stroke="#16a34a" stroke-width="2.5" marker-end="url(#arrow-green)" />
    <path d="M 287.5 235 L 287.5 280" fill="none" stroke="#16a34a" stroke-width="2.5" marker-end="url(#arrow-green)" />

    <!-- GPU Compute Block -->
    <rect x="50" y="285" width="320" height="150" fill="#ffffff" stroke="#94a3b8" stroke-width="1.5" rx="8" />
    <text y="305" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="11" font-weight="700" fill="#334155" x="210">Exécution en SRAM/registre</text>

    <!-- Pass Directly to Tensor Cores (Weights) -->
    <rect x="65" y="320" width="135" height="40" fill="#f0fdf4" stroke="#16a34a" stroke-width="1.5" rx="4" />
    <text y="337" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="10" font-weight="700" fill="#166534" x="132.5">Aucun surcoût de déquantification</text>
    <text y="350" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="9" fill="#166534" x="132.5">Alimentation directe depuis le matériel</text>

    <!-- Pass Directly to Tensor Cores (Activations) -->
    <rect x="220" y="320" width="135" height="40" fill="#f0fdf4" stroke="#16a34a" stroke-width="1.5" rx="4" />
    <text y="337" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="10" font-weight="700" fill="#166534" x="287.5">Aucun surcoût de déquantification</text>
    <text y="350" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="9" fill="#166534" x="287.5">Alimentation directe depuis le matériel</text>

    <!-- Arrow from Weights to Tensor Core -->
    <path d="M 132.5 360 L 132.5 388 C 132.5 400, 180 400, 195 400" fill="none" stroke="#16a34a" stroke-width="1.5" />

    <!-- Arrow from Activations to Tensor Core -->
    <path d="M 287.5 360 L 287.5 388 C 287.5 400, 240 400, 225 400" fill="none" stroke="#16a34a" stroke-width="1.5" />

    <!-- Math execution box -->
    <rect x="180" y="385" width="60" height="35" fill="url(#purple-grad)" rx="4" />
    <text y="406" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="10" font-weight="700" fill="#ffffff" x="210">INT8/FP8</text>
    <text y="445" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="10" fill="#334155" x="210">Exécuté directement sur le matériel</text>

    <!-- Bottom Note -->
    <rect x="50" y="450" width="320" height="40" fill="#f0fdf4" stroke="#16a34a" rx="6" />
    <text y="466" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="10" font-weight="600" fill="#166534" x="210">⚡ Accélération matérielle complète :</text>
    <text y="478" text-anchor="middle" font-family="Inter, system-ui, sans-serif" font-size="9.5" fill="#166534" x="210">Débloque un débit théorique du Tensor Core deux fois supérieur (TOPS).</text>
  </g>

  <!-- Markers -->
  <defs>
    <marker id="arrow-orange" viewBox="0 0 10 10" refX="6" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M 0 1.5 L 8 5 L 0 8.5 z" fill="#ea580c" />
    </marker>
    <marker id="arrow-blue" viewBox="0 0 10 10" refX="6" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M 0 1.5 L 8 5 L 0 8.5 z" fill="#2563eb" />
    </marker>
    <marker id="arrow-green" viewBox="0 0 10 10" refX="6" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M 0 1.5 L 8 5 L 0 8.5 z" fill="#16a34a" />
    </marker>
  </defs>
</svg>
